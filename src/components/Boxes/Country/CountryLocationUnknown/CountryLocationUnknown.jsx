import { Icon } from '../../../UI';

const CountryLocationUnknown = () => {

    return (
        <div>
            <Icon
                name="map-marked-alt"
            />
        </div>
    );
};

export default CountryLocationUnknown;