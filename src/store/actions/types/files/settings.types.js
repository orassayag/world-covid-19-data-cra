import { textUtils } from '../../../../utils';

const SettingsType = textUtils.createEnum([
    ['SETTINGS_SET_STATE_SETTINGS_LIST', 'settings_set_state_settings_list']
]);

export default SettingsType;